spring:
  application:
    name: member-service
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/retail_mono?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
server:
  port: 8082

jwt:
  secret: my-super-secret-key-for-retail-monorepo
  expiration: 3600000